"""Reporting: periods, aggregation, and the coverage every figure carries.

A consumer of `app`-layer data and nothing more. Nothing in `compliance/` or
`vision_os/` knows this package exists, and nothing here writes a row that
another surface reads — a report is a projection for a person, never a second
source of truth.
"""
